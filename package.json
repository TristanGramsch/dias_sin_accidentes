{
  "name": "ensayo-dias-sin-accidentes",
  "version": "1.0.0",
  "description": "Node.js application to track days without incidents - Docker ready for port 443 deployment",
  "main": "server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "docker:build": "docker build -f docker/Dockerfile -t dias-sin-accidentes:dev .",
    "docker:run": "docker run -d --name dias-dev-counter -p 3001:4443 --env-file docker/.env.dev --restart unless-stopped dias-sin-accidentes:dev",
    "docker:stop": "docker rm -f dias-dev-counter || true",
    "docker:logs": "docker logs -f dias-dev-counter",
    "docker:up": "npm run docker:build && npm run docker:run",
    "docker:down": "npm run docker:stop"
  },
  "keywords": [
    "safety",
    "counter",
    "incidents",
    "workplace",
    "docker",
    "raspberry-pi"
  ],
  "author": "Optoelectrónica Icalma",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "luxon": "^3.7.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "vitest": "^3.2.4"
  }
}
